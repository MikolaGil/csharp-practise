using System;

namespace Shape
{
    class Program
    {
        static void Main(string[] args)
        {
            MainShape mainShape = new MainShape();
            mainShape.GetLength();
            mainShape.MakeArray();
            mainShape.DisplayShape();
        }
    }
}
